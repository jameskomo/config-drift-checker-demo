schema_version: "1.1"
context:
  scaffold_script: |
    git init -q
    git config user.email "eval@example.com"
    git config user.name "Eval Bot"
    printf 'first line\n' > notes.txt
    git add notes.txt
    git commit -q -m "initial commit"
    printf 'second line\n' >> notes.txt
